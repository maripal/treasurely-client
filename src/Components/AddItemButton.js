import React from 'react';
import AddItemContainer from './AddItemContainer';

//This comp will be a button that will open up the addItemModal(still have to work out that functionality)
const AddItemButton = props => {
  return (
    <div>
      <AddItemContainer />
    </div>
  );
};

export default AddItemButton;